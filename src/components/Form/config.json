{
	"type": "Form",
	"name": "表单",
	"canPlace": true,
	"tips": "当点击 form 表单中 form-type 为 submit 的 button 组件时，会将表单组件中的 value 值进行提交，需要在表单组件中加上 name 来作为 key。",
	"defaultProps": {
		"reportSubmit": false
	},
	"defaultStyles": {
		"display": "block",
		"minHeight": "100px"
	},
	"config": [
		{
			"key": "reportSubmit",
			"label": "是否返回 formId 用于发送模板消息",
			"type": "Radio"
		}
	]
}
